{
  "Logging": {
    "LogLevel": {
      "Default": "Debug", //these are for user-defined logs
      "Microsoft.AspNetCore": "Information" //here HTTP logs are generated by asp.net core. so we have to set the minimum log level.
    },
    
      "EventLog": {
        "LogLevel": {
          "Default": "Debug"
        }
      }
  }
}
